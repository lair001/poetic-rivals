<%
value = nil if !local_assigns.has_key?(:value)
data = {} if !local_assigns.has_key?(:data)
form_id = nil if !local_assigns.has_key?(:form_id)
btn_id = nil if !local_assigns.has_key?(:btn_id)
btn_class = nil if !local_assigns.has_key?(:btn_class)
%>
<%= render partial: 'layouts/one_half_width_field_centered', locals: { content: (render partial: 'layouts/forms/form_tag/global_button_only_form', locals: { action: action, method: method, content: content, value: value, data: data, form_id: form_id, btn_class: btn_class, btn_id: btn_id }) } %>